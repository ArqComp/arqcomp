<%= form_for [:admin, @study] do |f| -%>
  <%= render :partial => "/shared/admin/error_messages", :locals => {
          :object => @study,
          :include_object_name => true
        } %>
  
  <div class='field'>
    <%= f.label :title, t(:'activerecord.attributes.studies.title') -%>
    <%= f.text_field :title, :class => 'larger widest'  -%>
  </div>

  <div class='field'>
    <%= f.label :study_category_id, t(:'activerecord.attributes.studies.study_category') -%>
    <%= f.select :study_category_id, @categorias, :class => 'larger widest'  -%>
  </div>
  
  <div class='field'>
    <%= f.label :link, t(:'activerecord.attributes.studies.link') -%>
    <%= render :partial => "/shared/admin/resource_picker", :locals => {
          :f => f,
          :field => :link_id,
          :resource => @study.link,
        } %>
  </div>
  
  <div class='field'>
    <div id='page-tabs' class='clearfix ui-tabs ui-widget ui-widget-content ui-corner-all'>
      <ul id='page_parts'>
        <% [:text].each_with_index do |part, part_index| %>
          <li class='ui-state-default<%= ' ui-state-active' if part_index == 0 %>'>
            <%= link_to t(part.to_s, :scope=>:'activerecord.attributes.studies'), "##{part}" %>
          </li>
        <% end %>
      </ul>

      <div id='page_part_editors'>
        <% [:text].each do |part| %>
          <div class='page_part' id='<%= part %>'>
            <%= f.text_area part, :rows => 20, :class => 'wymeditor widest' -%>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  
  <%= render :partial => "/shared/admin/form_actions",
              :locals => {
                :f => f,
                :continue_editing => false,
                :delete_title => t('delete', :scope => 'admin.studies.study'),
                :delete_confirmation => t('message', :scope => 'shared.admin.delete', :title => @study.title)
              } %>
<% end -%>
<% content_for :javascripts do %>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end %>
