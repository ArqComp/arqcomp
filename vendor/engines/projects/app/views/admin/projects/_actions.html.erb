<ul>
  <% if Admin::ProjectsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_projects_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_project_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::ProjectsController.sortable? and Project.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Projects"),
                 admin_projects_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Projects"),
                 admin_projects_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>