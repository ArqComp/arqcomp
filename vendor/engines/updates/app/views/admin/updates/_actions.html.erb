<ul>
  <% if Admin::UpdatesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_updates_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_update_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::UpdatesController.sortable? and Update.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Updates"),
                 admin_updates_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Updates"),
                 admin_updates_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>