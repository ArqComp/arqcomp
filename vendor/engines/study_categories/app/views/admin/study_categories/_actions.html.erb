<ul>
  <% if Admin::StudyCategoriesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_study_categories_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_study_category_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::StudyCategoriesController.sortable? and StudyCategory.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Study Categories"),
                 admin_study_categories_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Study Categories"),
                 admin_study_categories_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>